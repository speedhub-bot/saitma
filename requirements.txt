python-telegram-bot[job-queue]>=20.0
# Pyrofork is a drop-in replacement for pyrogram with several download
# fixes (bigger chunk sizes, unstuck FloodWait, faster file references)
# and ships under the same `pyrogram` import name. tgcrypto is the C
# implementation of Telegram's MTProto crypto — without it downloads
# fall back to a pure-Python AES-IGE implementation that is ~5-10x
# slower.
pyrofork>=2.3.45
tgcrypto>=1.2.0
aiosqlite>=0.19.0
python-dotenv>=1.0.0
patool>=1.12
tqdm>=4.60.0
apscheduler>=3.10.0
loguru>=0.7.0
aiohttp>=3.9.0
# Required by services/url_downloaders/mega_nz.py for in-process AES-CTR
# streaming of mega.nz downloads (no `megacmd` binary needed).
pycryptodome>=3.20.0
